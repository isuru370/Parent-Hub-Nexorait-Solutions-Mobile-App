import 'package:flutter/material.dart';

class AppColors {

  // =========================================================
  // BRAND COLORS (LOGO COLORS)
  // =========================================================

  /// Main Education Brand Color
  /// AppBar, Primary Buttons, Active Tabs
  static const Color primaryBlue = Color(0xFF0EA5E9);

  /// Dark Blue Accent
  /// Headers, Icons, Statistics Cards
  static const Color darkBlue = Color(0xFF1D4ED8);

  /// Main Orange Accent
  /// Payment Module, CTA Buttons, Highlights
  static const Color primaryOrange = Color(0xFFF97316);

  /// Gold Accent
  /// Premium Features, Badges, Achievements
  static const Color primaryGold = Color(0xFFFACC15);

  // =========================================================
  // BACKGROUND COLORS
  // =========================================================

  /// Main App Background
  static const Color background = Color(0xFFF8FAFC);

  /// Card Background
  static const Color surface = Color(0xFFFFFFFF);

  /// Secondary Background
  static const Color lightBackground = Color(0xFFF1F5F9);

  // =========================================================
  // TEXT COLORS
  // =========================================================

  /// Main Text
  static const Color textPrimary = Color(0xFF0F172A);

  /// Secondary Text
  static const Color textSecondary = Color(0xFF64748B);

  /// Disabled Text
  static const Color textDisabled = Color(0xFF94A3B8);

  /// White Text
  static const Color textWhite = Color(0xFFFFFFFF);

  // =========================================================
  // STATUS COLORS
  // =========================================================

  /// Success Messages
  static const Color success = Color(0xFF22C55E);

  /// Warning Messages
  static const Color warning = Color(0xFFF59E0B);

  /// Error Messages
  static const Color error = Color(0xFFEF4444);

  /// Information Messages
  static const Color info = Color(0xFF3B82F6);

  // =========================================================
  // ATTENDANCE COLORS
  // =========================================================

  /// Present
  static const Color present = Color(0xFF22C55E);

  /// Absent
  static const Color absent = Color(0xFFEF4444);

  /// Late
  static const Color late = Color(0xFFF59E0B);

  // =========================================================
  // PAYMENT COLORS
  // =========================================================

  /// Paid
  static const Color paid = Color(0xFF22C55E);

  /// Pending
  static const Color pending = Color(0xFFF97316);

  /// Overdue
  static const Color overdue = Color(0xFFDC2626);

  // =========================================================
  // DASHBOARD MODULE COLORS
  // =========================================================

  /// Attendance Card
  static const Color attendanceCard = Color(0xFF0EA5E9);

  /// Results Card
  static const Color resultsCard = Color(0xFF22C55E);

  /// Payments Card
  static const Color paymentsCard = Color(0xFFF97316);

  /// Notices Card
  static const Color noticesCard = Color(0xFF8B5CF6);

  /// Profile Card
  static const Color profileCard = Color(0xFF1D4ED8);

  // =========================================================
  // BORDERS
  // =========================================================

  /// TextField Border
  static const Color border = Color(0xFFE2E8F0);

  /// Focus Border
  static const Color focusedBorder = Color(0xFF0EA5E9);

  /// Divider Color
  static const Color divider = Color(0xFFE5E7EB);
}